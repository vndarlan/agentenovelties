from setuptools import setup, find_packages

setup(
    name="browser-agent",
    version="0.1.0",
    packages=find_packages(),
    include_package_data=True,
    install_requires=[
        # Dependências já definidas em requirements.txt
    ],
)